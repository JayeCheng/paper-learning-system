# Daily Reports

Daily reports are the human-readable paper radar archive.

The intended pair is:

- `YYYY-MM-DD.md` for long-term reading
- `YYYY-MM-DD.json` or `data/public/daily-YYYY-MM-DD.json` for structured consumers

Markdown should stay useful to humans. Frontends should use JSON.
