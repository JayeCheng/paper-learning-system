from __future__ import annotations


def publish_to_notion(*, report_path: str) -> dict:
    """Placeholder for Notion publishing.

    Notion is a downstream presentation layer and must not become primary storage.
    """

    return {"enabled": False, "report_path": report_path}
