from __future__ import annotations


def fetch_github_candidates(*, query: str | None = None, limit: int = 20) -> list[dict]:
    """Return GitHub project candidates.

    v0.0 intentionally avoids implementing network fetching.
    """

    return []
