from __future__ import annotations


def fetch_arxiv_candidates(*, query: str | None = None, limit: int = 20) -> list[dict]:
    """Return arXiv candidates.

    v0.0 intentionally avoids implementing network fetching.
    """

    return []
