from __future__ import annotations


def fetch_biorxiv_candidates(*, query: str | None = None, limit: int = 20) -> list[dict]:
    """Return bioRxiv candidates.

    v0.0 intentionally avoids implementing network fetching.
    """

    return []
