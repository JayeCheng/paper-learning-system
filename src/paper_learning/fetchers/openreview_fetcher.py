from __future__ import annotations


def fetch_openreview_candidates(*, venue: str | None = None, limit: int = 20) -> list[dict]:
    """Return OpenReview candidates.

    v0.0 intentionally avoids implementing network fetching.
    """

    return []
